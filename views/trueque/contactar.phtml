<div class="container mt-5">
    <?= View::content() ?>
    <?= Html::includeCss('https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css') ?>

    <div class="card shadow-lg p-4 rounded-4">
        <h2 class="text-center mb-3">💬 Contactar al Propietario</h2>

        <p><strong>Artículo:</strong> <?= htmlspecialchars($trueque->nombre) ?></p>
        <p><strong>Propietario:</strong> <?= htmlspecialchars($usuario->nombre) ?></p>
        <p><strong>Correo de contacto:</strong> <?= htmlspecialchars($usuario->correo) ?></p>

        <form method="post" action="">
            <div class="mb-3">
                <label for="mensaje" class="form-label">Mensaje</label>
                <textarea id="mensaje" name="mensaje" class="form-control" rows="4" placeholder="Escribe tu mensaje ecológico..."></textarea>
            </div>
            <button type="submit" class="btn btn-success w-100 rounded-3">🌱 Enviar Mensaje</button>
        </form>

        <div class="text-center mt-3">
            <a href="<?= PUBLIC_PATH ?>trueque/ver/<?= $trueque->id ?>" class="btn btn-outline-secondary rounded-3">⬅️ Volver</a>
        </div>
    </div>
</div>


